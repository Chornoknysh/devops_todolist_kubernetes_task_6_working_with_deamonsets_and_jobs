apiVersion: batch/v1
kind: CronJob
metadata:
  name: todoapp-healthcheck
  namespace: todoapp
spec:
  schedule: "*/4 * * * *"
  concurrencyPolicy: Allow
  successfulJobsHistoryLimit: 10
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: healthcheck
            image: ikulyk404/busyboxplus:curl
            resources:
              requests:
                cpu: "50m"
                memory: "64Mi"
              limits:
                cpu: "100m"
                memory: "128Mi"
            command: ["/bin/sh"]
            args:
            - "-c"
            - "curl -s http://todoapp-clusterip.todoapp.svc.cluster.local/api/health"
          restartPolicy: OnFailure
